is http

struct AnotherResp {
  fn get_something() :: String {
    ret "Hello!"
  }
}

struct HttpResponse {
  text :: String,
  str :: AnotherResp

  fn get_text() :: AnotherResp {
    ret str
  }
}


fn get(url :: String) :: http:HttpResponse {
  ret make HttpResponse("text", make AnotherResp())
}