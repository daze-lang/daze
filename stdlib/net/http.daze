is http

struct AnotherResp {
  something :: String
  fn get_something(something :: String) :: String {
    ret "Hello {something}!"
  }
}

struct HttpResponse {
  text :: String,
  str :: AnotherResp

  fn get_text() :: AnotherResp {
    ret str
  }
}


fn get(url :: String) :: HttpResponse {
  ret make HttpResponse("text", make AnotherResp("Field value!"))
}