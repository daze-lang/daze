is main

use "daze::io"
use "daze::net::http"

struct Person {
  name :: String

  fn greet() :: String {
    ret "Hello, {name}"
  }
}

fn main() :: Int {
  response :: http:HttpResponse := http:get("http://google.com")
  # response = "Hello"
  io:println("Hello")
}
