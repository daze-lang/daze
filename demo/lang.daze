struct Person {
  name :: String
}

fn new_person(name :: String) :: Person {
  ret make Person(name);
}

fn (Person) get_name() :: String {
  ret self.name;
}

fn main() :: Int {
  p := new_person("Mark") |> .get_name;
  p |> out;
}