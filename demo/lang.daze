is Main;

use "daze::Std";

fn fib(num :: Int32, a :: Int32) :: Int32 {
  if (num <= 1) {
    ret 1;
  }

  ret fib(num - 1) + fib(num - 2);
}


fn main() :: Void {
  res = if (true) {
    "a";
  } else {
    "b";
  }
  Std.out("Fibonacci of 42 is: #{res}");
}