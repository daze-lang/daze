is main;

use "daze::http";
use "daze::io";

fn main() :: Int {
  response :: http:HttpResponse := http:get(
    "http://jsonplaceholder.typicode.com/posts/1"
  );
  io:println(response.get_text());
}