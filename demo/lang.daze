struct Person {
  name :: String
}

implement Person {
  fn get_name() :: String {
    ret self.name;
  }
}

fn str(s :: String) :: String {
  ret s + " is nice.";
}

fn main() :: Int {
  me := make Person("Mark");
  s := "s";
  out(type(me));
  out(s);
}