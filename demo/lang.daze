fn greet(name :: String) :: String {
  ret "Hello, " + name;
}

fn say(s :: String) :: Void {
  out(s);
}

struct Person {
  name :: String
}

implement Person {
  fn get_name() :: String {
    ret self.name;
  }
}

fn main() :: Int {
  make Person("Mark") |> .get_name |> greet |> say;
}