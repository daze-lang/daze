is main;

use "daze::io";

fn range(from :: Int, to :: Int) :: []Int {
  arr :: []Int := {};
  i :: Int := from;

  for (i <= to) {
    arr <- i;
    i++;
  }

  ret arr;
}

fn main() :: Int {
  for i in range(10, 15) {
    io:println(i);
  }
}