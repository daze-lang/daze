is main;

use "daze::io";

global NAME = "Mark";
global MILLISECOND = 1;
global SECOND = MILLISECOND * 60;

struct Person {
  name :: Int

  fn get_name() :: Int {
    ret name;
  }
}

fn main() :: Int {
  h :: Int := 5;
  b :: Person := make Person(h);
  io:println(SECOND);
}