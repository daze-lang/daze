is main;

use "daze::io";
use "daze::term";
use "daze::http";
use "./fibonacci";

fn main() :: Int {
  name :: String := "Mark";
  # fib_of_30 :: Int := fibonacci:fibonacci(1);
  io:println(http:get("http://daze.one"));
}