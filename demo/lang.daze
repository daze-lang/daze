is main;

use "daze::io";
use "./fibonacci";

struct Person {
  name :: String
}

struct Container {
  p :: Person
}

fn main() :: Int {
  # cont :: Container := make Container(make Person("Mark"));
  io:println(fibonacci:fibonacci(46));
}