is Main;

use "daze::Std";

fn fib(num :: Int32, a :: Int32) :: Int32 {
  if (num <= 1) {
    ret 1;
  }

  ret fib(num - 1) + fib(num - 2);
}


fn main() :: Void {
  for c in []String{1, 2, 3} {
    Std.out(c);
  }

  for c in "hello".split("") {
    Std.out(c);
  }
}