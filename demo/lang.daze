is main;

use "daze::io";

struct Person {
  name :: String
}

fn changeName(p :: ref Person) :: Void {
  p.name = "Carl";
}

fn main() :: Int {
  p :: ref Person := &make Person("Mark");
  io:println(p.name); # Mark
  changeName(p);
  io:println(p.name); # Carl
}