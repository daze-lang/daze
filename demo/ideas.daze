fn say_hello(s :: String) :: String {
  ret "Hello, " + s;
}

fn main() :: Int {
  my_name := "mark";

  out(my_name |> capitalize |> say_hello);

  result := when something {
    is "aa" { "a" }
    is "bb" { "b" }
    is "cc" { "c" }
    is "dd" { "d" }
  }
}