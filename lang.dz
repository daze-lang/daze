// Main module definition
is main;

use "testModule";

fn name() :: Int32 {
  ret 5;
}

// This is the main function, the entry point of the program.
fn main() :: Void {
  var := 5 + 10 + TestModule.get_num();
  out(var);
}